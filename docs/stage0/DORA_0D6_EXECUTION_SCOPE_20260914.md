# Stage 0D.6 scoped execution instruction — 14 September 2026

Subsequent bounded amendment: the [E36 campaign owner decision](DORA_0D6_ALPHA_E36_CAMPAIGN_OWNER_DECISION_20260914.md)
now authorizes the prepared 414 E36 base entries and protocol-compliant invalid
hard-kill replacements without mandatory human review or routine repeat permission.
This separate campaign path supersedes the prior stop-after-preflights instruction.
Technical checks, evidence applicability, exact ownership, retention, negative-case
product outcomes and full physical/API requirements remain unchanged. The earlier
preflight-only decision and the following historical scope text are preserved.

Current bounded amendment: the [internal alpha owner decision](DORA_0D6_ALPHA_PREFLIGHT_OWNER_DECISION_20260914.md)
removes the mandatory human-review prerequisite for the three named E36-GAPI preflights.
Technical gates remain required. This task stops at preflight results and campaign handoff;
it does not execute the 414 E36 campaign entries. The earlier conditional campaign scope
below is historical context and is not admitted by the alpha-preflight exception.

This additive record summarizes the project owner's current plan-and-execute instruction for
Stage **0D.6 — Device preflight and fault campaign**. The complete instruction, implementation
plan, closure matrix and raw evidence are retained in the private work package. Earlier owner
decisions, consumed launch authorizations and historical readiness snapshots remain unchanged.

The owner authorizes necessary scoped Recovery fixes, source preservation, publication through
the normal branch/PR/review/CI workflow, technical preparation, and protocol-defined synthetic
fault and app-process hard-kill tests on task-owned emulators **after each operation's applicable
source, review, environment and authority prerequisites are satisfied**. This instruction supplies
conditional emulator campaign authority; it does not assert that those prerequisites have passed.
Physical execution requires the exact device, operation and applicable owner authorization.
No merge, direct-main development, force push, host reboot/power cut, unidentified process kill,
personal-data access or hardware purchase is authorized by this scope.

The effective contract is the accepted v0.8 result boundary, its inherited v0.7 streaming
persistence contract and the inherited v0.6 fault semantics, with older base provisions retained
where those amendments do not override them. The canonical 46 fault templates, 184 Phase A
template executions, 138 full-physical template executions and 120 hard-kill attempts per
candidate remain historical protocol metadata. Per-candidate expansion applies COR-04/COR-05
only to microfiles: 44 streaming plus 46 microfile branches, yielding 360 Phase A fault
executions (270 emulator, 90 D2) and 270 full-physical fault executions. Independent execution
packet review must confirm this expansion before launch. Hard-kill denominators are separate:
120 base attempts per candidate with the protocol's environment/stratum validity minima and
explicit invalid-replacement rules. No automatic rerun may conceal a valid failure.

The immutable successful attempt `REC-I3-4375B82-GRADLE-FIRST-05` remains bound solely to source
`4375b829db7dffef66036e5589e3ffe5961733dc` and its original APKs. Its one passing E36-GAPI test
proves that method's bounded SQLite/readback/replay/identity-denial assertions. It is not complete
Recovery preflight, authenticated payload campaign or process-death evidence. Its noncanonical
compile-options digest and missing historical raw cleanup diagnostics are not reconstructed or
silently corrected. Successor diagnostic and campaign evidence must use new identities.

The accepted API33+ boundary for the current PoC remains documented in
[the SQLite decision](../REC-I3-SQLITE-PER-CONNECTION-POC-DECISION.md). Application minSdk remains
28. The full D1 profile's API28–30 requirement still needs a compatible scoped implementation or
an explicit owner profile/scope decision; API36 emulator evidence cannot close it.

Implementation and independent host verification may continue while a physical device or
nondelegable review is unavailable. Missing implementation is not an external blocker. Exact
complete REC-I3 accountable implementation/evidence review remains mandatory and cannot be
substituted by an AI advisory. Existing REC-I2A graph/Product-IP authority may be reused only
after an exact graph/package/R8 comparison; the older REC-I2B human review covers its original
source, not the complete successor.

The old readiness checker intentionally continues to refuse the historical REC-I1-era packet.
Current launch admission is additive and must bind the immutable source/APKs, protocol/fixture,
review artifacts and owned environment. CI source recognition is not execution approval.

The implementation successor adds real Tink streaming publication, explicit Android fault and
kill-boundary adapters, run-scoped evidence retention, an independent host oracle, deterministic
campaign manifests and strict instrumentation completion parsing. Supplemental SQLite and
Keystore/filesystem diagnostics are separate tests; the original attempt05 method is preserved.
Cleanup admission requires complete retained evidence. Recipe completion and product acceptance
are separate predicates, and a valid failure is never converted to an invalid replacement.

Local successor verification passed 350 Recovery JVM tests, 39 host campaign/parser tests,
Android instrumentation compilation, formatting, whole-Recovery Detekt, debug lint, debug/test
APK assembly, release R8, owned graph resolution and crypto-policy checks. Android compilation
does not establish device behavior. The graph check exposed 16 missing existing-coordinate
memberships for `debugAndroidTestLintChecksClasspath`; the scoped lock repair adds only those
memberships, with no dependency/version change. Failed invocations and subsequent corrections
remain in private evidence. Exact immutable source identities, final package comparison and
CI results are recorded separately after source freeze; no successor device campaign has run.

`POC-RECOVERY-001`, full Recovery and 0D.6 remain **NOT PASSED** until every applicable criterion
has valid evidence and required approval. Phase A cannot yield full Recovery PASS. Mandatory
physical evidence cannot be substituted with emulators. Later production admission is outside
this work. Private raw evidence, source bundles, archives and SHA-256 manifests are delivered to
the existing private Recovery work hierarchy; only sanitized descriptions enter this repository.
