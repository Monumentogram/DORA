# REC-I3 reconciliation review correction — Stage 0 v0.1

Status: Selected for bounded correction implementation; independent/accountable review pending.

Authority: OD-15 and `OWNER-AUTH-BATCH-20260819-01`. Correction base:
`dbbde0ef8e16cbac6cbe6f2ccbefde43e1853188`. This additive record responds to the independent
`REVISE` disposition (P0/P1/P2 = 0/9/1) without changing the authority, execution or admission
ceiling of the original reconciliation scope and ADR.

## Corrected boundary

The Android composition owns the unified-journal and descriptor-backed filesystem source and its
production entry accepts only a run identity plus the already selected evidence sink. Complete
typed active/quarantine inventory and pending-intent enumeration feed confirmation-first
reconciliation. Internal constructors retain injected test ports; no caller-supplied production
snapshot can mint authority.

Typed outcomes preserve the exact confirmation/envelope priority, safe-parent/unsafe-leaf split,
KCB-05 bound/path/crypto failures, manifest structural/authentication/operational failures and
quarantine load/begin/inspect/fsync/commit uncertainty. Results carry immutable diagnostic values,
never mutable `Throwable` objects. Prefix authorization recomputes candidate, manifest and ordered
row identity digests. Fallback records rejected newer state and validates complete manifest/run/
candidate/previous-digest linkage. Quarantine supports unique-source conflict readback, pending
replay after a moved source, every active/destination state and complete immutable row comparison.

The Android path adapter validates every ancestor by `lstat`, applies role-specific descriptor
bounds and guarantees closure. Schema v3 is validated on create, upgrade and open; object scans
consider both object name and attached table. The host verifier exercises user-version migration,
rollback, nullable bootstrap binding, duplicates and the complete declared constraint matrix.

Evidence and status must state only assertions backed by the final acceptance table. All ten
Recovery blockers, `fullRecI3Completed=false`, preflight lock, execution/measurement/admission
false values and prior immutable evidence remain unchanged.

## Additional exact files

Create:

- `android/poc/recovery/src/main/kotlin/com/monumentogram/dora/poc/recovery/candidate/RecoveryReconciliationOutcomes.kt`
- `android/poc/recovery/src/main/kotlin/com/monumentogram/dora/poc/recovery/journal/AndroidRecoveryReconciliationSource.kt`
- `android/poc/recovery/src/test/kotlin/com/monumentogram/dora/poc/recovery/candidate/RecoveryReconciliationAcceptanceTest.kt`
- `android/poc/recovery/src/test/kotlin/com/monumentogram/dora/poc/recovery/journal/AndroidRecoveryReconciliationSourceTest.kt`
- `android/poc/recovery/src/test/kotlin/com/monumentogram/dora/poc/recovery/storage/AndroidOsRecoveryReconciliationStorageTest.kt`

The correction may modify only the files already admitted by the original reconciliation scope,
this correction record, the current reconciliation evidence, backlog/status and matching successor
validator/tests. It must not edit the reviewed confirmation controller, predecessor bootstrap/
publication source or evidence, dependencies, locks, R8, manifests or normative protocol files.

## Required correction evidence

Meaningful RED/GREEN cases must map every independent finding and every original required-evidence
row: production identity-only composition, confirmation priority, KCB-05 bound/path/exception,
typed taxonomy, real-Tink N-to-N-1 fallback, unit failure at first/middle/last, candidate/manifest/
row proof forgery, five path states, active inventory and pending replay, Q01/Q05 unknown outcomes,
unique-source conflict, descriptor bounds/zero-progress/closure/ancestor symlink, same-run exclusion,
different-run independence, 721/overflow limits, exact v3 open/create/migrations/rollback/constraints
and immutable diagnostics. Platform syscall/journal fakes are host evidence only; Android runtime
durability remains gated.
